{% extends('base.twig') %}

{% block body %}

    <section class="admin__section--comments">
        {% for comment in comments %}
            <ul>
                <li>{{ comment.content }}</li>
            </ul>
        {% endfor %}
    </section>
{% endblock %}
