{% extends('base.twig') %}

{% block body %}
    <section class="admin__section--posts">
        {% for post in posts %}
            <ul>
                <li>{{ post.title }}</li>
            </ul>
        {% endfor %}
    </section>
{% endblock %}
