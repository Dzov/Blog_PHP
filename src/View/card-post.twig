<div class="post-preview">
    <a href="{{ asset('posts/' ~ post.id) }}">
        <h2 class="post-title">
            {{ post.title }}
        </h2>
        <h3 class="post-subtitle">
            {{ post.subtitle }}
        </h3>
    </a>
    <p class="post-meta">Modifié le
        {{ post.updatedAt | date("d, M Y") }} par {{ post.username | capitalize }}</p>
</div>
<hr>
